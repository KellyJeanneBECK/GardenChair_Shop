{% extends 'base.html.twig' %}

{% block title %}New City{% endblock %}

{% block body %}
    <section class="container">
        <h1>Create new City</h1>

        {{ include('city/_form.html.twig') }}

        <a href="{{ path('app_city_index') }}" class="btn btn-outline-primary mt-2">back to list</a>
    </section>
{% endblock %}
